{
  "name": "@rop/scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "demo": "tsx src/demo.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo No scripts tests"
  },
  "dependencies": {
    "@rop/shared": "workspace:*",
    "dotenv": "^16.4.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
